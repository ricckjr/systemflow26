Este diretório é legado.

A fonte oficial de migrations do projeto fica em `backend/src/db/migrations/`.

Os arquivos SQL aqui foram movidos para `db/deprecated/` apenas como referência histórica e não devem ser executados em ambientes reais.
